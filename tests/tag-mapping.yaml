# Master Tag Mapping — Capability & Project Focused
# Tags are applied as: [project-group, type, capabilities..., domains...]
# Languages are tracked in frontmatter metadata (not as Obsidian tags)
# Graph coloring: by project-group for repo identity, by capability for discovery

# ---- Hermes ecosystem ----
hermes:
  project: hermes
  features: [agent-gateway, messaging, multi-platform]
  languages: [python, typescript, rust]
  # Files: 26 total

hermes-agent-docker:
  project: hermes
  features: [packaging, container-deployment]
  languages: [docker]
  # Files: 2 (wiki, deployment)

# ---- OpenClaw ecosystem ----
openclaw:
  project: openclaw
  features: [agent-gateway, messaging, personal-assistant, live-canvas]
  languages: [typescript, swift]
  # Files: 11 total

# ---- Podman ecosystem ----
podman:
  project: podman
  features: [container-engine, daemonless, rootless, oci]
  languages: [golang]
  # Files: 6 total

buildah:
  project: buildah
  features: [image-builder, oci, daemonless]
  languages: [golang]
  # Files: 4 total

podlet:
  project: podlet
  features: [quadlet-generator, systemd, oci-conversion]
  languages: [rust]
  # Files: 4 total

crun-vm:
  project: crun-vm
  features: [oci-runtime, vm, container-runtime, qemu]
  languages: [rust]
  # Files: 3 total

sablier:
  project: sablier
  features: [scale-to-zero, reverse-proxy, lifecycle, daemon]
  languages: [golang]
  # Files: 4 total

# ---- AgentField ecosystem ----
agentfield:
  project: agentfield
  features: [control-plane, identity, orchestration, harness]
  languages: [golang]
  # Files: 6 core + 4 sub-repos

swe-af:
  project: agentfield
  features: [swe, engineering-factory, multi-agent, code-generation]
  languages: [python]
  # Files: 3

sec-af:
  project: agentfield
  features: [security, auditor, adversarial, vulnerability-hunting]
  languages: [python]
  # Files: 3

af-deep-research:
  project: agentfield
  features: [deep-research, recursive-agents, research]
  languages: [python]
  # Files: 3

af-reactive-atlas-mongodb:
  project: agentfield
  features: [reactive, real-time, atlas-triggers, mongodb]
  languages: [python]
  # Files: 3

# ---- Mission Control ----
mission-control:
  project: mission-control
  features: [dashboard, orchestration, monitoring]
  languages: [typescript, nextjs, react]
  # Files: 7 total

# ---- n8n ----
n8n:
  project: n8n
  features: [workflow-automation, low-code, integration, fair-code]
  languages: [typescript, vue]
  # Files: 7 total

# ---- Gogs ----
gogs:
  project: gogs
  features: [git, self-hosted, version-control, ssh]
  languages: [golang]
  # Files: 4 total

# ---- Hermzner ----
hermzner:
  project: hermzner
  features: [infrastructure-as-code, hetzner, vps, deployment-automation]
  languages: [terraform, ansible]
  # Files: 4 total

# ---- Tank OS ----
tank-os:
  project: tank-os
  features: [immutable-os, container-os, image-based, bootc]
  languages: [bootc, fedora]
  # Files: 4 total

# ---- Nix Podman Stacks ----
nix-podman-stacks:
  project: nix-podman-stacks
  features: [infrastructure-as-code, declarative, podman, self-hosted-stacks]
  languages: [nix, home-manager]
  # Files: 3 total

# ---- ClawPier ----
clawpier:
  project: clawpier
  features: [desktop, agent-manager, gui, cross-platform]
  languages: [rust, tauri]
  # Files: 4 total

# ---- Shared / cross-cutting ----
shared:
  project: shared
  features: [deployment, architecture, landscape, knowledge-base]
  languages: []
  # Files: 5 total
