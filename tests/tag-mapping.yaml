# Master Tag Mapping — Tech & Feature Focused
# Each project as a set of tags applied to ALL its files
# Format: project-slug: [project-tag, type-tag?, tech-tags..., feature-tags...]

# ---- Hermes ecosystem ----
hermes:
  project: hermes
  tech: [typescript]
  features: [agent-gateway, messaging, multi-platform]
  # Files: 26 total

hermes-agent-docker:
  project: hermes
  tech: [docker]
  features: [packaging, container-deployment]
  # Files: 2 (wiki, deployment)

# ---- OpenClaw ecosystem ----
openclaw:
  project: openclaw
  tech: [typescript]
  features: [agent-gateway, messaging, personal-assistant, live-canvas]
  # Files: 11 total

# ---- Podman ecosystem ----
podman:
  project: podman
  tech: [golang]
  features: [container-engine, daemonless, rootless, oci]
  # Files: 6 total

buildah:
  project: buildah
  tech: [golang]
  features: [image-builder, oci, daemonless]
  # Files: 4 total

podlet:
  project: podlet
  tech: [rust]
  features: [quadlet-generator, systemd, conversion]
  # Files: 4 total

crun-vm:
  project: crun-vm
  tech: [rust, qemu]
  features: [oci-runtime, vm, container-runtime]
  # Files: 3 total

sablier:
  project: sablier
  tech: [golang]
  features: [scale-to-zero, reverse-proxy, lifecycle]
  # Files: 4 total

# ---- AgentField ecosystem ----
agentfield:
  project: agentfield
  tech: [golang]
  features: [control-plane, identity, orchestration, harness]
  # Files: 6 core + 4 sub-repos

swe-af:
  project: agentfield
  tech: [typescript]
  features: [swe, engineering-factory, multi-agent]
  # Files: 3

sec-af:
  project: agentfield
  tech: [typescript]
  features: [security, auditor, adversarial]
  # Files: 3

af-deep-research:
  project: agentfield
  tech: [typescript]
  features: [deep-research, recursive-agents, research]
  # Files: 3

af-reactive-atlas-mongodb:
  project: agentfield
  tech: [typescript, mongodb]
  features: [reactive, real-time, atlas-triggers]
  # Files: 3

# ---- Mission Control ----
mission-control:
  project: mission-control
  tech: [typescript, nextjs, react]
  features: [dashboard, orchestration, monitoring]
  # Files: 7 total

# ---- n8n ----
n8n:
  project: n8n
  tech: [typescript, vue]
  features: [workflow-automation, low-code, integration, fair-code]
  # Files: 7 total

# ---- Gogs ----
gogs:
  project: gogs
  tech: [golang]
  features: [git, self-hosted, version-control, ssh]
  # Files: 4 total

# ---- Hermzner ----
hermzner:
  project: hermzner
  tech: [terraform, ansible]
  features: [iac, hetzner, vps, deployment-automation]
  # Files: 4 total

# ---- Tank OS ----
tank-os:
  project: tank-os
  tech: [bootc, fedora]
  features: [immutable-os, container-os, image-based]
  # Files: 4 total

# ---- Nix Podman Stacks ----
nix-podman-stacks:
  project: nix-podman-stacks
  tech: [nix, home-manager]
  features: [iac, declarative, podman, self-hosted-stacks]
  # Files: 3 total

# ---- ClawPier ----
clawpier:
  project: clawpier
  tech: [rust, tauri]
  features: [desktop, agent-manager, gui]
  # Files: 4 total

# ---- Shared / cross-cutting ----
shared:
  project: shared
  tech: []
  features: [deployment, architecture, landscape]
  # Files: 5 total
